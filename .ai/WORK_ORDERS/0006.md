# 施工單 0006 — 條文到測試的追溯矩陣（讓「完整性」可被機器驗證）

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** CLOSED
**依據：** [GAP_REGISTER.md](../GAP_REGISTER.md) G6。既有 `blueprint-coverage`／`requirements-coverage`／`roadmap-gate-audit` 只驗「證據檔案存在」，不驗每條 MUST 是否被實作與測試。

## 1. 硬性邊界

不改 `electron/` 執行邏輯、不放寬 SecurityPolicy、不新增權限、不改 `Blueprint/`（Claude 已 append 決策註記，勿動）、不 merge PR #7、不 force push、不標任何 ENVIRONMENT / OWNER 項目完成。**不得為了讓稽核通過而虛報 IMPLEMENTED。**

## 2. 目標

建立 `.ai/TRACEABILITY.json` 與 `scripts/traceability-audit.cjs`，把藍圖的規範性條文逐條對應到**可驗證的證據**，並納入 `npm run verify`。

### 2.1 條文清單（每條一個 item）

至少涵蓋：`Blueprint/REQUIREMENTS.md` 的 R1.1–R8.6 全部；`20` §27 第 1–20 條；`18` §13 第 1–15 條；`22` §16 第 1–8 條與 §5–§11 各節的 must／required 句；`21` 交接規則（10 條）與 §6–§7 各 schema；`11` 的 schema 與大小限制；`01` 的無障礙目標與 must 句；`04` 的 Electron 基線與 RED 操作規則；`05`、`14`、`15`、`16`、`02`、`03` 內所有以 must／shall／never／required／不得／必須 表述的規範句。**由你逐檔讀藍圖抽出，不得只抄本單列的清單。**

### 2.2 每個 item 的欄位

- `id`、`source`（檔案 + 章節）、`text`（原文摘錄或精簡）
- `status` ∈ `IMPLEMENTED`｜`PARTIAL`｜`GAP`｜`ENVIRONMENT`｜`OWNER`｜`DELIBERATE_NON_GOAL`
- `evidence`：陣列，每筆 `{file, kind: "test" 或 "code", name}`
- `workOrder`：GAP／PARTIAL 必填，例如 `0007` 或 `UNASSIGNED`
- `note`

### 2.3 稽核規則（script 必須機械檢查）

1. 每個 item 都有合法 `status`；`IMPLEMENTED` 至少一筆 `kind: "test"` 證據；只有 `kind: "code"` 證據時只能標 PARTIAL。
2. 證據檔案存在；`kind: "test"` 的 `name` 必須在該測試檔中以 `test(`／`it(` 字串出現；`kind: "code"` 的 `name` 必須以識別字出現在該檔案中。
3. `GAP`／`PARTIAL` 必須有 `workOrder`；若引用的施工單在 `.ai/WORK_ORDERS/` 中狀態為 `CLOSED`，而 item 仍是 GAP／PARTIAL → 稽核失敗（逼你更新）。`UNASSIGNED` 允許，但輸出警告清單。
4. `ENVIRONMENT`／`OWNER` 必須引用 `.ai/ACCEPTANCE.md` 第 5 節或 `Reports/*RUNBOOK*.md` 的對應段落。
5. 輸出各 status 的數量，以及所有 `GAP`／`PARTIAL`／`UNASSIGNED` 清單。**已知 GAP 必須如實標記：**
   - 20 §27 #6 → `0007`
   - 20 §27 #19 → `0008`
   - 21 的 `aecp.review/v1`、`aecp.worker-report/v1` → `0007`
   - 22 §6、§7、§8、§11 與 20 §16 進度總覽 → `0009`
   - 22 §9 欄位完整度 → `0009`
6. 在 `package.json` 加入 `audit:traceability` 並串入 `verify`。`scripts/blueprint-coverage.cjs` 的輸出加註「此稽核只驗證證據檔案存在，不驗證條文實作；條文追溯見 traceability-audit」，但**不得刪除**它。

### 2.4 測試

`tests/traceability-audit.test.cjs` 涵蓋：合法矩陣 PASS；缺證據檔 FAIL；`IMPLEMENTED` 無 test 證據 FAIL；test 名稱不存在 FAIL；GAP 無 workOrder FAIL；GAP 指向已 CLOSED 施工單 FAIL；未知 status FAIL。

## 3. 交付與回報

分項獨立 commit、push、`npm run verify` 全綠、更新 `.ai/STATUS.md`、填「Codex 回報」並改 `DONE-PENDING-REVIEW`。

**回報必須包含：**
- 矩陣各 status 統計；
- 所有 `GAP`／`PARTIAL`／`UNASSIGNED` 清單，特別是本單發現、GAP_REGISTER 未列出的**新落差**，每條附條文出處與你看到的程式碼證據；
- 你判定為 IMPLEMENTED 的項目中，**最不確定的 10 條與理由**，讓 Claude 抽查。

做完 0006 後接續 0007。

## Codex 回報

**狀態：** DONE-PENDING-REVIEW。0006 施工 commit：見此檔 Git history；本機 `npm run verify` PASS（新追溯測試 7/7；總測試數見 STATUS，CI 另查 exact HEAD）。

追溯矩陣 [TRACEABILITY.json](../TRACEABILITY.json) 共 231 條：IMPLEMENTED 24、PARTIAL 1、GAP 206、ENVIRONMENT 0、OWNER 0、DELIBERATE_NON_GOAL 0。此數字代表**證據分類**，不是完成率；未找到同條文具名測試者保守列 GAP，**並非斷言功能全未實作**。UNASSIGNED 185 條；完整出處、原文與可查證證據在矩陣。已知 G1–G5 對應 0007–0009。

GAP 全部 ID（下列 UNASSIGNED 為待逐條裁定的測試／實作缺口，不得冒稱已完成）：
- B01（13）：B01-A11Y-L172、B01-A11Y-L174、B01-A11Y-L175、B01-A11Y-L176、B01-A11Y-L177、B01-A11Y-L178、B01-A11Y-L179、B01-A11Y-L180、B01-L148、B01-L160、B01-L27、B01-L6、B01-L67
- B02（5）：B02-L109、B02-L156、B02-L161、B02-L207、B02-L95
- B03（10）：B03-L113、B03-L116、B03-L129、B03-L143、B03-L168、B03-L172、B03-L5、B03-L56、B03-L68、B03-L70
- B04（31）：B04-ELECTRON-L28、B04-ELECTRON-L29、B04-ELECTRON-L30、B04-ELECTRON-L31、B04-ELECTRON-L32、B04-ELECTRON-L33、B04-ELECTRON-L34、B04-ELECTRON-L35、B04-ELECTRON-L36、B04-ELECTRON-L37、B04-ELECTRON-L38、B04-L103、B04-L11、B04-L112、B04-L129、B04-L13、B04-L141、B04-L157、B04-L179、B04-L185、B04-L193、B04-L196、B04-L5、B04-RED-L65、B04-RED-L66、B04-RED-L67、B04-RED-L68、B04-RED-L69、B04-RED-L70、B04-RED-L71、B04-RED-L72
- B05（7）：B05-L118、B05-L132、B05-L20、B05-L34、B05-L5、B05-L88、B05-L93
- B11（12）：B11-4-CONTEXT、B11-5-RESULT、B11-6-TRACE、B11-7-CLIPBOARD、B11-8-L136、B11-8-L137、B11-8-L138、B11-L103、B11-L44、B11-L46、B11-L53、B11-L61
- B14（20）：B14-L15、B14-L165、B14-L193、B14-L263、B14-L292、B14-L298、B14-L349、B14-L419、B14-L438、B14-L5、B14-L88、B14-REQUIRED-L167、B14-REQUIRED-L168、B14-REQUIRED-L169、B14-REQUIRED-L170、B14-REQUIRED-L171、B14-REQUIRED-L172、B14-REQUIRED-L173、B14-REQUIRED-L174、B14-REQUIRED-L175
- B15（8）：B15-L117、B15-L12、B15-L291、B15-L324、B15-L40、B15-L61、B15-L84、B15-L90
- B16（9）：B16-L171、B16-L190、B16-L229、B16-L233、B16-L286、B16-L292、B16-L335、B16-L85、B16-L91
- B18（3）：B18-13-13、B18-13-14、B18-13-15
- B20（21）：B20-14-MEMORY、B20-15-AGENTS、B20-16-OVERVIEW、B20-27-01、B20-27-02、B20-27-03、B20-27-04、B20-27-05、B20-27-06、B20-27-07、B20-27-08、B20-27-10、B20-27-11、B20-27-12、B20-27-14、B20-27-15、B20-27-16、B20-27-17、B20-27-18、B20-27-19、B20-27-20
- B21（10）：B21-10-01、B21-10-02、B21-10-03、B21-10-04、B21-10-05、B21-10-06、B21-10-07、B21-10-08、B21-6-SCHEMA、B21-7-SCHEMA
- B22（14）：B22-10-EVENT、B22-11-NOTIFY、B22-16-02、B22-16-03、B22-16-04、B22-16-05、B22-16-06、B22-16-07、B22-16-08、B22-5-FIELDS、B22-6-TIMELINE、B22-7-DIFF、B22-8-REVIEW、B22-9-GITHUB
- R1（3）：R1.1、R1.2、R1.4
- R2（6）：R2.1、R2.2、R2.3、R2.4、R2.6、R2.7
- R3（8）：R3.1、R3.10、R3.2、R3.3、R3.5、R3.6、R3.7、R3.9
- R4（6）：R4.1、R4.3、R4.4、R4.5、R4.6、R4.7
- R5（4）：R5.1、R5.2、R5.4、R5.5
- R6（5）：R6.1、R6.2、R6.3、R6.4、R6.5
- R7（5）：R7.1、R7.2、R7.3、R7.4、R7.5
- R8（6）：R8.1、R8.2、R8.3、R8.4、R8.5、R8.6

PARTIAL 全部 ID：B12-L134。新落差 `B12-L134`（Blueprint/12 §4）：藍圖要求每個持久文件有 `schemaVersion`；`electron/lib/state-migration.cjs` 有該欄位，但 `electron/lib/event-ledger.cjs` 寫入的是 `schema`，尚未證明語意等價及遷移覆蓋。屬本單發現、GAP_REGISTER 未列，未擴大施工；請 Claude 決定是否另立單。另一待驗證落差：Blueprint/01 §10 的 4.5:1 對比、Windows 125–200% scaling 等，現有測試僅有鍵盤／focus／reduced motion，無視覺或對比值證據；列 GAP/UNASSIGNED，未臆測達標。

最不確定的 10 條 IMPLEMENTED 與原因：
1. B18-13-01：spec 驗證測試未涵蓋所有未來 adapter。
2. B18-13-05：worktree 隔離測試只覆蓋 fake Worker。
3. B18-13-06：與 #5 共用測試，未覆蓋真實 provider。
4. B18-13-10：patch 測試有上限斷言，但二進位邊界仍需抽查。
5. B18-13-12：Apply 明確測試只覆蓋 API，UI 人工流程未驗。
6. B20-27-13：失敗次數預算測試不等於所有預算與 timeout 全覆蓋。
7. B22-16-01：一屏八問的靜態測試不是視覺驗收。
8. R1.5：能力矩陣測試不等於所有 RED 動作端到端核准。
9. R3.8：隔離 CODEX_HOME 測試未證明真實 auth 永不交叉。
10. R5.3：事件投影測試未證明所有 Dashboard 欄位均正確。

偏差：施工單稱 Blueprint/21 §10 有 10 條，原文明列 8 條；矩陣以原文 8 條為準。矩陣對未驗明的條文採保守分類，後續須逐條用具名測試、實機或 owner 證據縮減 UNASSIGNED；此時不能宣稱「藍圖完全實作」。

## Claude Code 驗收（2026-09-24，HEAD `c98ad0f`）— CLOSED

獨立核對：本地與遠端同為 `c98ad0f`；該 SHA 四組 workflow 皆 success（Security／CI／Packaging／Build and Release）；我本機重跑 `npm run verify` exit 0，326 測試（325 通過、1 因本機無 python 跳過）。
- 追溯矩陣共 231 條：52 IMPLEMENTED／1 PARTIAL／178 GAP。稽核腳本規則嚴謹（`test(`／`it(` 名稱以正規式比對、`IMPLEMENTED` 必須有 test 證據、GAP 必須有 workOrder、指向已 CLOSED 施工單即失敗），並已串入 `npm run verify`。
- 我抽查 9 條 IMPLEMENTED（B18-13-01/06/11、B20-27-06/19、B21-6-SCHEMA、B22-16-02/07、B22-8-REVIEW）：證據測試真實存在且主題相符。**弱點：** `B22-16-01..08` 共 9 條指向同一個大型測試，粒度偏粗；後續 0010／0011 要求每條 IMPLEMENTED 的測試必須針對該條文。
- **重要判定：這 178 條 GAP 是「尚未分類」，不是「已證實缺失」。** 例如 R1.1（不碰 ChatGPT DOM）幾乎必然已由設計達成，只是沒有對應測試。它們必須逐條分成：已實作（補測試）、部分／確認缺失、需人工驗證、環境／擁有者。這正是 0010、0011 的工作。
- Codex 主動揭露 `B12-L134`（`schemaVersion` 與事件帳本使用 `schema` 是否等價）與 Blueprint/01 §10 對比度、125–200% 縮放僅有鍵盤／焦點證據：誠實，接受。
