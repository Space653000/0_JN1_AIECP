# 施工單 0015 — 補審藍圖 06、07、08、24 並納入追溯矩陣

**簽發：** Claude Code → **執行：** Sonnet 5（施工者）｜ **日期：** 2026-09-25 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** OPEN
**前置：** 0014 完成。**驗收方式：** `scripts/review-gate.cjs --mode tests`（若本單發現真缺陷，**只標 `CONFIRMED_GAP`，不修**）。

## 0. 為什麼有這張單

追溯矩陣目前涵蓋 B01–B05、B11、B12、B14–B16、B18、B20–B22 與 R1–R8，**藍圖 06（桌面／ChatGPT 整合）、07（安裝／發行）、08（路線圖／驗收）、24（本地模型供應商驗證）尚未納入**，所以「對標藍圖完整」對這四份沒有任何證據。本單補齊。

## 1. 規則

同 0013 的規則：只動 `tests/`、`.ai/`（不含 `ACCEPTANCE.md`）；測試必須載入真實產品程式；每條 `IMPLEMENTED` 都要有 `.ai/sabotage-log.jsonl` 破壞紀錄（破壞 `electron/` 或 `ui/` 或 `scripts/` 產品程式）；不確定就標 `PARTIAL`；`ENVIRONMENT` 只限需要真實機器／帳號／憑證；`MANUAL` 只限需要真人看畫面；`OWNER` 只限需要擁有者憑證／身分。

## 2. 做法

1. 逐份讀 `Blueprint/06_*.md`、`07_*.md`、`08_*.md`、`24_*.md`，抽出所有 must／shall／never／required／不得／必須 規範句。
2. 在 `.ai/TRACEABILITY.json` 新增 item（比照既有欄位：`id`（如 `B06-L12`）、`source`（檔案＋章節，章節名須存在於該檔）、`text`、`status`、`evidence`、`note`），先全部標 `PARTIAL`，再逐條依 0013 的流程處理。**確認 `node scripts/traceability-audit.cjs --strict` 通過。**
3. 08 的 P0–P7 階段閘門：每個階段的每個「gate」bullet 也是一條；已由 `scripts/roadmap-gate-audit.cjs` 等既有稽核覆蓋者，證據引用該稽核的測試，不重複寫。
4. 07（安裝／發行）與 24（本地模型）多半屬 ENVIRONMENT／OWNER：分類並引用 `.ai/ACCEPTANCE.md` 第 5 節或 `Reports/*RUNBOOK*.md`，不要標 IMPLEMENTED。
5. 完成後在本檔追加回報：各狀態統計、所有 `CONFIRMED_GAP`（附 file:symbol 與缺什麼）、你最不確定的 10 條。

## 回報

（尚未開始）
