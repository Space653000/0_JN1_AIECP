# 施工單 0015 — 補審藍圖 06、07、08、24 並納入追溯矩陣

**簽發：** Claude Code → **執行：** Sonnet 5（施工者）｜ **日期：** 2026-09-25 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** CLOSED
**前置：** 0014 完成。**驗收方式：** `scripts/review-gate.cjs --mode tests`（若本單發現真缺陷，**只標 `CONFIRMED_GAP`，不修**）。

## 0. 為什麼有這張單

追溯矩陣目前涵蓋 B01–B05、B11、B12、B14–B16、B18、B20–B22 與 R1–R8，**藍圖 06（桌面／ChatGPT 整合）、07（安裝／發行）、08（路線圖／驗收）、24（本地模型供應商驗證）尚未納入**，所以「對標藍圖完整」對這四份沒有任何證據。本單補齊。

## 1. 規則

同 0013 的規則：只動 `tests/`、`.ai/`（不含 `ACCEPTANCE.md`）；測試必須載入真實產品程式；每條 `IMPLEMENTED` 都要有 `.ai/sabotage-log.jsonl` 破壞紀錄（破壞 `electron/` 或 `ui/` 或 `scripts/` 產品程式）；不確定就標 `PARTIAL`；`ENVIRONMENT` 只限需要真實機器／帳號／憑證；`MANUAL` 只限需要真人看畫面；`OWNER` 只限需要擁有者憑證／身分。

## 2. 做法

1. 逐份讀 `Blueprint/06_*.md`、`07_*.md`、`08_*.md`、`24_*.md`，抽出所有 must／shall／never／required／不得／必須 規範句。
2. 在 `.ai/TRACEABILITY.json` 新增 item（比照既有欄位：`id`（如 `B06-L12`）、`source`（檔案＋章節，章節名須存在於該檔）、`text`、`status`、`evidence`、`note`），先全部標 `PARTIAL`，再逐條依 0013 的流程處理。**確認 `node scripts/traceability-audit.cjs --strict` 通過。**
3. 08 的 P0–P7 階段閘門：每個階段的每個「gate」bullet 也是一條；已由 `scripts/roadmap-gate-audit.cjs` 等既有稽核覆蓋者，證據引用該稽核的測試，不重複寫。
4. 07（安裝／發行）與 24（本地模型）多半屬 ENVIRONMENT／OWNER：分類並引用 `.ai/ACCEPTANCE.md` 第 5 節或 `Reports/*RUNBOOK*.md`，不要標 IMPLEMENTED。
5. 完成後在本檔追加回報：各狀態統計、所有 `CONFIRMED_GAP`（附 file:symbol 與缺什麼）、你最不確定的 10 條。

## 回報

### 回報（2026-09-25，Sonnet 5 自動連跑；閘門 `--mode tests --ci` ACCEPTED）
- 基準 ff89200；commit：6ba93c9（首版）、e0855d2（修正版，閘門通過）；npm run verify exit 0，測試 709 → 720。
- **範圍：** 藍圖 06（13 條）、07（約 45 條）、08（117 條 Deliverable／Acceptance／Required 逐 bullet）、24（約 27 條）共 **210 條**新增至 `.ai/TRACEABILITY.json`（231 → 441）。**狀態統計（新增 210）：121 IMPLEMENTED、52 PARTIAL、15 ENVIRONMENT、10 OWNER、7 MANUAL、3 DELIBERATE_NON_GOAL、2 CONFIRMED_GAP。** 全矩陣：320 已實作／59 部分／4 確認缺失／16 人工／17 ENVIRONMENT／14 OWNER／11 蓄意不做。
- **做法：** IMPLEMENTED 條文引用「載入真實產品程式」的既有或新增測試；新增 `roadmap-gate-contracts`（tools:detect 真實偵測、app:info 架構、Workspace 跨重啟保存、Ollama 設定契約、Codex 沙箱旗標、協定 schema 獨立於 app 版本、瀏覽器停靠需核准）與 `electron-shell-and-modes`（真 main.cjs 的視窗選項與導覽／開新窗守衛、真 preload、真 UI 的執行模式就緒度）；fake Electron 新增會記錄的 BrowserWindow。90 筆破壞紀錄由閘門逐筆重放且全部 DETECTED；許多條文共用同一批破壞（entry 的 ids 陣列列出全部），共用測試以 `covers` 標記。
- **CI 級證據不冒充：** 需要安裝程式／CI 打包才能證明的條文（07 的安裝行為與發行自動化、08 的 Store 打包、P4.6 的 CI 證明）標為 PARTIAL 並寫明「CI-level evidence only」；需要真機／真帳號／憑證者為 ENVIRONMENT／OWNER；需要人看的為 MANUAL（ACCEPTANCE §6）。
- **CONFIRMED_GAP（新 2 條，皆已實測、未修）：** ①**B24-L8** `electron/lib/provider-router.cjs`（`ollama` 註冊表）：藍圖 24 寫 Ollama 有 Builder 角色，程式只有 planner／reviewer／general（故意 fail-closed）→ 需擁有者決定（G20）。②**B06-L31** `ui/app.js`（dockBrowserLeftButton）：UI 沒教 Windows Snap、也不記住窗格版面（G19）。另外 08 的 P1–P4.7 逐 bullet 沒有再發現新缺陷；R8.4（Definition of Done 不顯示）為 0013 批次 11 發現（G21）。
- **閘門過程：** 第一次 REJECTED——①B08-L7/L14/L15/L81/L85/L88 引用的 `chatgpt-untouched-integration`、`human-controls` 是原始碼掃描型測試，不載入產品程式（閘門正確指出）；②AECP CI 在 Windows 上一個既有測試 `B03-L113` 在遍歷 userData 時遇到原子寫入的暫存檔被改名而 ENOENT（競態）。修正：新增真實視窗／preload／執行模式測試並改引用；四個檔案遍歷測試改為略過 `.tmp` 暫存檔。第二次 ACCEPTED。
- **我最不確定的判斷：** ①P0–P4 的許多 Deliverable 我判為 IMPLEMENTED 是因為「同一行為」已由更細的條文測試證明（例如 B08-L8 本地持久化 = B11-6-TRACE 的重啟測試），但它們字面上比條文窄或寬；審查者若要求逐字對應可退回。②B08-L46「危險動作被擋或需核准」只用 PUSH 風險等級被降為 GREEN 這一種破壞來守；分級表其他項目靠既有測試。③三十幾條 PARTIAL 是「行為存在且有測試，但本輪沒有為它做破壞檢查」——是否值得補由審查者決定。④B07 安裝行為（每使用者、Start Menu、解除安裝保留資料）全歸 PARTIAL(CI)，因為它們是 package.json／NSIS 設定，閘門不接受對 package.json 的破壞。⑤B08-L158（裝置撤銷）的破壞檢查會讓測試卡住而非失敗，所以標 PARTIAL（一個 sabotage-check 對「測試程序不結束」的已知限制）。
