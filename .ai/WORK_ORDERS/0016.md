# 施工單 0016 — 收尾：把可以自動證明的「部分」轉為已實作，並修剩下的兩個已知缺陷

**簽發：** Claude Code → **執行：** Sonnet 5（施工者）｜ **日期：** 2026-09-26 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** OPEN
**驗收方式：** 每個單位跑 `node scripts/review-gate.cjs --base <SHA> --mode <tests|fix> --ci`（協議見 `.ai/AUTONOMOUS_RUN.md`）。

## 0. 目前狀態

矩陣 441 條：323 已實作、59 部分、16 人工、17 環境、14 擁有者、12 蓄意不做、確認缺失 0。**本單只處理那 59 條「部分」中能用自動測試證明的部分，以及兩個已知缺陷。** 人工／環境／擁有者項目不在範圍內，**不得標為完成**。

## 1. 單位 A（`--mode tests`）— 19 條「只差破壞紀錄」的 B08 條文

`B08-L38 L39 L54 L56 L65 L69 L73 L98 L100 L101 L108 L118 L125 L126 L128 L129 L139 L151 L158`：它們的 `note` 都寫「行為已由某條測試證明，但沒有為它記錄破壞檢查」。逐條：
1. 找到 `note` 所指的既有測試與產品程式碼。
2. 選一個**該條文獨有**的破壞點（破壞 `electron/`、`ui/` 或產品 `scripts/` 的一小段），用 `node scripts/sabotage-check.cjs` 確認 `DETECTED`，追加到 `.ai/sabotage-log.jsonl`（`ids` 放這條 ID）。
3. `NOT DETECTED` 就代表既有測試守不住這條：**補一個針對該條文的測試**再檢查；補了仍守不住才維持 `PARTIAL` 並寫明原因。
4. 更新矩陣：`PARTIAL` → `IMPLEMENTED`，`evidence` 指向真正載入產品程式的測試。
（`B08-L158` 先前破壞會讓測試卡住：先給該測試加逾時使其失敗而非卡死。）

## 2. 單位 B（`--mode tests`）— 設定值型的 07／08 條文

`B07-L5 L15 L21 L53–L61 L85 L98 L114`、`B08-L164 L168 L183`：這些條文的字面內容是「打包設定／工作流程有某個設定」（每使用者安裝、Start Menu 捷徑、解除安裝項目、不安裝背景服務、權限最小化…）。對「**字面上就是設定值**」的條文（與 `contextIsolation: true` 同類）：
- 寫測試**解析真實的 `package.json` 的 `build` 設定、`.github/workflows/*.yml`（用 YAML／JSON 解析，不要用原始碼正規式）**並斷言該設定值，載入產品設定檔即算載入產品程式。
- 破壞紀錄可破壞 `package.json`（閘門已允許）或 `scripts/`；**不可破壞 `.github/`**（閘門禁止修改該目錄）。無法用破壞證明的維持 `PARTIAL`。
- **「安裝後真的這樣運作」這種行為（例如真的建立捷徑、真的保留資料）不是設定值，仍是 CI／環境證據，維持 `PARTIAL`，不要虛報。**

## 3. 單位 C（`--mode fix`）— 兩個已知缺陷

1. **G17（SecurityPolicy 路徑正規化）：** `electron/lib/security-policy.cjs` 與 `path-safety.cjs`：在文字比較前先做 `fs.realpath`（目錄不存在時對最近的既有祖先做 realpath 再接續其餘片段），使 Windows 8.3 短名、junction／symlink 別名與大小寫差異不會讓合法路徑被拒，**且不放寬任何拒絕結果**：逃出允許根目錄的路徑（含經 junction 逃出者）仍必須被拒。先寫紅測試（短名／junction 別名合法路徑目前失敗；junction 逃逸應被拒），再最小修復。完成後可移除 `tests/support/canonical-tmp.cjs` 對 `npm test` 的預載——**只有在移除後 `verify` 與 CI 仍全綠時才移除**，否則保留並在回報說明。
2. **received／correlated 崩潰窗口：** `electron/lib/control-plane.cjs` `ingestExternalEvent`：兩次寫入之間行程崩潰會使 `correlated` 永遠補不回來。改為重啟後可補寫：啟動時（或下次 ingest 時）找出「有 `external.received` 但沒有對應 `external.correlated`」的事件並補寫，仍保持去重與冪等。測試用真 ControlPlane、在兩次寫入之間模擬中止再重啟。

## 4. 單位 D（`--mode tests`）— 其餘 15 條雜項與 `B05-L34`、`B05-L118`

`B02-L207 B11-L61 R3.9 B07-L44 B08-L6 L26 L37 L67 L79 L80 L89 L120 L132 L166 L186`：逐條看 `note` 的「缺什麼」。能用自動測試補上就補（並附破壞紀錄）；屬環境／人工／擁有者的改標成正確的狀態並引用 `ACCEPTANCE.md` 第 5、6 節；發現真缺陷標 `CONFIRMED_GAP`（不要在本單位修）。`B05-L34`（`GitHubGateway` 未被使用且呼叫不存在的 gh 子命令）與 `B05-L118`（`client_payload` 未解析）若是真缺陷，屬單位 C 風格的修補，請在單位 C 之後另起一個 `fix` 單位處理，附紅測試與破壞紀錄。

## 5. 硬性規則

同 `.ai/AUTONOMOUS_RUN.md`：不改 `Blueprint/`、`.github/`、`.ai/ACCEPTANCE.md`；不放寬安全檢查或既有斷言；不標任何環境／擁有者／人工項目為完成；測試須在 Linux 與 Windows 都通過；不 merge PR、不 force push。

## 6. 完成時

更新 `.ai/STATUS.md` 頂部結論的最終統計，追加各單位回報，列出仍為 `PARTIAL` 的條文與**逐條原因**（你不能自動證明的）與你最不確定的 10 個判斷，然後停下。

## 回報

（尚未開始）
