# 施工單 0018 — 產品介面改成 ChatGPT／Codex 風格（顏色與版面）

**簽發：** Claude Code → **執行：** Sonnet 5（施工者）｜ **日期：** 2026-09-26 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** DONE-PENDING-REVIEW
**前置：** 0017 完成。**驗收方式：** `node scripts/review-gate.cjs --base <SHA> --mode fix --ci`。

## 0. 為什麼

擁有者喜歡 ChatGPT 與 Codex 桌面版的顏色與版面，要求 AECP 的網頁與所有呈現給人看的介面盡量仿照。參考已完成的範例：`artifacts/dashboard/index.html` 的 `<style>` 區塊（深色近黑中性底、柔和圓角卡片、單一綠色強調、等寬字體用於程式碼與 ID、淺色主題為白底與淺灰卡片）。

## 1. 範圍

- **只改視覺**：`ui/styles.css`（與必要時 `ui/index.html` 的版面容器／class，不改任何 `id`、`data-*`、`aria-*`、`data-i18n` 與既有 class 名稱，避免破壞既有 UI 測試與腳本）。**不改任何功能行為、IPC、權限。**
- **色彩 token 化**：所有顏色改由 `:root` 的 CSS 變數決定（若還沒有），並定義兩組主題：
  - 深色（預設，跟隨系統）：背景 `#212121`、卡片 `#2f2f2f`、次要面 `#3a3a3a`、邊線 `#424242`、文字 `#ececec`、次要文字 `#b4b4b4`、強調綠 `#19c37d`；狀態色：成功綠、警告琥珀 `#f5b74f`、錯誤紅 `#ff6b6b`。
  - 淺色：背景 `#ffffff`、卡片 `#f7f7f8`、邊線 `#e5e5e5`、文字 `#0d0d0d`、次要文字 `#6e6e80`、強調綠 `#10a37f`。
  - 保留既有的 `data-theme` 與 `ui/theme.js` 的 system／dark／light 行為，只換 token 值。
- **版面風格**：窄置中的內容欄（約 860px 以內）、卡片圓角 12–16px、細邊線、大留白、按鈕與標籤為圓角膠囊、輸入框圓角並有柔和聚焦環、等寬字體用於 ID／程式碼／路徑；側邊面板可維持現有結構，但外觀比照 Codex 的左側欄（暗色、無粗邊框、選中項用低對比底色高亮）。
- **商標與品牌邊界（必守）**：**只仿顏色與版面感覺，不使用 OpenAI／ChatGPT／Codex 的標誌、字標、圖示或字型檔。** 藍圖 01 規定 AECP 自己的名稱與識別必須比任何供應商更醒目；不要出現會讓人以為是 OpenAI 產品的文字或圖示。

## 2. 無障礙與測試（不得倒退）

- 深色與淺色兩組主題中，一般文字與背景對比 **≥ 4.5:1**、大字與元件邊界 **≥ 3:1**；狀態不得只靠顏色（保留文字或圖示）；鍵盤焦點可見；`prefers-reduced-motion` 仍生效。
- 新增測試 `tests/ui-theme-contrast.test.cjs`：**解析真實的 `ui/styles.css`** 取出兩組主題的 token，用 WCAG 公式計算下列配對的對比並斷言達標：文字／背景、次要文字／背景、次要文字／卡片、強調色（用於文字時）／背景、三種狀態色／卡片。破壞紀錄：把某個 token 改成低對比值（例如把深色文字改成 `#555555`），測試必須失敗。
- 既有測試（`dashboard-visibility-ui`、`theme-behavior`、`i18n-accessibility`、`electron-shell-and-modes` 等）全部照過，不改斷言。
- 用 `npm start`（若本機有 Electron）或 `ui/` 加假 bridge（可參考 `tests/support/ui-harness.cjs`、`artifacts/ui-check/serve.cjs`）在瀏覽器實際看過深色與淺色兩種畫面；**在回報附上你看到的描述**（哪些區塊、哪些狀態、有沒有版面跑掉）。
- 矩陣：把 `B01-A11Y-L172`（4.5:1 對比）的 `note` 更新為「token 對比已由自動測試驗證；已安裝應用程式的實機觀察仍為 MANUAL」，**狀態維持 MANUAL**（你不能替它結案）。

## 3. 完成時

`npm run verify` 全綠、閘門 ACCEPTED（含 CI）、`.ai/STATUS.md` 更新、本檔追加回報（含深淺色畫面描述）並改 `DONE-PENDING-REVIEW`。

## 回報

### 完成
**改了什麼**：只動 `ui/styles.css`（沒有改 `index.html`、任何 id／data-*／aria-*／data-i18n／class 名稱，也沒有改任何 JS）。
- 兩組主題 token（深色預設、`data-theme="light"` 淺色）：深色 `#212121／#2f2f2f／#3a3a3a／#424242／#ececec／#b4b4b4／#19c37d`，淺色 `#ffffff／#f7f7f8／#e5e5e5／#0d0d0d／#6e6e80／#10a37f`。新增 `--sidebar`、`--accent-fg`（把綠色當文字用時的較深版本，淺色 `#0b7a5e`，因為 `#10a37f` 在白底只有約 3.2:1）、`--bad-text`、`--line-strong`（控制項邊界，≥3:1）與 `--ring`。舊有 token 名稱全部保留，`--blue` 現在指向綠色強調（單一強調色）。
- 版面層：左右側欄用較深／較淺的側欄底且無粗邊線；內容欄置中、最大 900px；卡片 12–16px 圓角；按鈕、分頁與標籤為圓角膠囊；輸入框圓角並有柔和聚焦環；程式碼、路徑、ID 使用等寬字體；AECP 的標誌與名稱加粗並用綠色強調。
- 緊急停止（`#hcEmergency`）改為紅色，避免看起來像友善的綠色主要按鈕（標籤色也用 token 並受對比測試檢查）。
- 順手修了既有版面問題：Harness 主控台的事件列、表單與卡片網格會被長文字撐出視窗（`.control-pane` 的 scrollWidth 1658 對 clientWidth 705），改用 `minmax(0,1fr)` 與 `min-width:0` 後為 705／705，沒有水平溢出。
- 不使用任何 OpenAI／ChatGPT／Codex 的標誌、字標、圖示或字型檔；畫面上的 AECP 名稱與標誌是最醒目的識別。

**測試**：`tests/ui-theme-contrast.test.cjs`（14 個）解析真實 `ui/styles.css`：兩組主題各自的文字／次要文字／faint 文字對背景、卡片、側欄與 panel-3 ≥4.5:1；強調色當文字與三種狀態色對背景與卡片 ≥4.5:1；主要按鈕標籤對強調色 ≥4.5:1、強調色對背景 ≥3:1；控制項邊界 ≥3:1；緊急停止標籤 ≥4.5:1；兩組主題 token 集合一致；調色盤與擁有者指定值相同；主題區塊以外沒有寫死的色碼（只剩 skip-link 的兩個 var() 後備值）。既有 UI 測試未改。破壞紀錄 6 筆（把深色文字改成 `#555555`、把淺色次要文字、淺色強調文字、淺色警告色、控制項邊界與緊急停止標籤色改成低對比值），皆 DETECTED。
矩陣：`B01-A11Y-L172` 的 note 已更新，狀態維持 MANUAL。

**我在瀏覽器實際看到的**（`artifacts/ui-check/serve.cjs` 加假 bridge，1440×900，用 Browser 窗格；不是已安裝的 Electron 應用程式）：
- 深色：近黑中性底，左側欄比主區更暗，右側欄同樣暗；首次使用的歡迎對話框是圓角大卡片、綠色膠囊主要按鈕、三個步驟列為圓角低對比卡；頂部左上角 AECP 標誌（綠色細框與綠色橫線）與粗體產品名最醒目；分頁（開始／Harness／看板…）是膠囊分段控制，選中項為較亮的灰底；Harness 主控台的卡片、狀態標籤（READY 綠、ACTION REQUIRED 琥珀、UNKNOWN 灰、RUNNING）都有文字，不只靠顏色；表單輸入框圓角、細邊框；STOP ALL 是紅色。
- 淺色：白底、淺灰側欄與卡片，深色文字，綠色只用在按鈕、標籤與焦點；狀態標籤同樣帶文字；STOP ALL 為紅底白字；版面與深色相同，沒有跑掉。
- 有沒有跑掉：修完後 Harness 主控台沒有水平溢出。我沒有選 Workspace（假 bridge 沒有工作區），所以沒看到有任務資料的看板卡片，只在 Harness 主控台看到有資料的區塊；也沒看 Providers 對話框與淺色的歡迎對話框。鍵盤焦點環我只靠 CSS 與既有測試確認，沒有實際 Tab 過。

