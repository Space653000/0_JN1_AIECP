# 施工單 0002

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** CLOSED

## 0. 背景

0001 已由 Claude 驗收 CLOSED。Runbook 列出的缺口中，有兩項可由倉庫施工補齊（證據等級仍是 ENVIRONMENT，程式碼只是讓操作者「能產出」證據）：
- 缺口 2：單 Worker OFFICIAL / PEGA / local-command smoke 沒有獨立檔案 verifier（只驗輸出 token）。
- 缺口 4（部分）：沒有故障隔離 mode。驗收協定見 `.ai/ACCEPTANCE.md` 第 5 節「PEGA/OFFICIAL 故障隔離」。

不在本單範圍（需擁有者/環境，不得動）：實體 ARM64 UI、筆電 verifier 審查、Official Full MCP、ARM64 專屬 runner 標籤、合併 PR #7。

## 1. 硬性邊界

- 不得放寬 SecurityPolicy、不得新增權限；不得把任何 ENVIRONMENT / OWNER 項目標完成；不得 merge PR #7、不得 force push、不得改 Blueprint 00–24。
- 故障注入**只能**透過該次程序的環境變數（例如給 PEGA 無效 key），**不得**修改、刪除或覆寫任何已存的 `auth.json` / `credentials.json` / `CODEX_HOME` 內容。
- 不得讓 evidence JSON 含憑證、prompt、response 內容；`privacy` 三個布林值須維持 false，且沿用既有 provenance 驗證。

## 2. 施工項 A — 單 Worker smoke 加獨立檔案 verifier

檔案：`scripts/provider-environment-verify.cjs`（及其測試）。參考現有 `multi-codex` / `opencode-ollama` 模式如何做「確定性檔案驗證」，讓 `codex-official`、`codex-pega`、`local-command` 的 smoke 也在**隔離暫存 worktree** 中要求 worker 建立一個內容可預期的檔案，由 script（而非 worker 自述）讀檔驗證，並在 check 中新增 `fileVerifier: { path, sha256, expectedSha256Match }`（不含檔案內容）。

驗收（TESTED）：以 fixed local worker / 假 provider 的確定性測試覆蓋：檔案正確 → PASS；worker 宣稱成功但未寫檔 → FAIL；內容錯 → FAIL。既有測試維持通過。

## 3. 施工項 B — 新增 `codex-fault-isolation` mode

在 `scripts/provider-environment-verify.cjs` 與 `.github/workflows/provider-environment.yml`（mode 選項與 `tests/provider-environment-workflow.test.cjs` 的斷言同步）新增 mode `codex-fault-isolation`，行為：
1. 讀前後兩次 `codex-official` 與 `codex-pega` 的 `codexHomeSha256`。
2. 階段 (a)：PEGA 程序環境注入無效 key，預期 PEGA check = FAIL（安全 error code），同時 OFFICIAL 真實 smoke = PASS。
3. 階段 (b)：OFFICIAL 程序以無效/空的隔離 `CODEX_HOME`（僅本次暫存副本，不碰真實 home）執行，預期 OFFICIAL = FAIL，PEGA = PASS。
4. 斷言：兩個 home hash 前後不變、無靜默 fallback、Worker Registry 狀態呈現失敗方 UNAVAILABLE/UNKNOWN 而另一方 READY；整體 PASS 的定義是「預期的失敗確實發生且被隔離」。
5. 產出遵循既有 `aecp.provider-environment-evidence/v1` schema（若需新增欄位，用向後相容方式並更新 provenance 驗證與測試）。

驗收（TESTED）：用 fake provider / 注入式 runner 的確定性測試覆蓋「失敗被隔離 → PASS」與「失敗外溢（另一方也失敗或 home hash 變動）→ FAIL」；workflow 測試涵蓋新 mode。不需真實網路/憑證。

## 3.5 Runbook 同步

更新 `Reports/ENVIRONMENT_EVIDENCE_RUNBOOK.md`：第 1、2、6 節的 verifier 缺口改為已補（附新欄位）；第 10 節改寫為使用新 mode 的操作步驟與證據欄位，並保留「仍需擁有者實機執行才算 ENVIRONMENT PASS」。未實作的部分不得寫成已完成。

## 4. 交付要求

1. A → B → runbook 同步，各自獨立 commit（英文簡短訊息），push 到本分支。
2. `npm run verify` 全綠；更新 `.ai/STATUS.md`（完成項、測試數、各 SHA 與該 SHA 實際 workflow 結果）。
3. 填寫本檔「Codex 回報」，狀態改 `DONE-PENDING-REVIEW`。CI 未跑完標「CI 待驗證」。
4. 需要 Blueprint 層級決策或新增權限時，停下來提問，不要自行決定。

## Codex 回報

**Codex 施工結果（2026-09-24）**：A `7d098e09be09c9beba15921bfca21ec4a427566d`，B `b97746109b07addf57d4aa138971f9123eb759c8`，runbook `a0b2a343cbce4d6064a48925a4bcd8bc6c22368f`。A 以 fake provider 的正確／缺檔／錯內容及固定 local worker 整合測試覆蓋獨立 verifier；B 以 fake router/暫存 WorkerRegistry 測預期故障隔離、外溢失敗、fallback 與共用 home 拒絕。施工後 `npm run verify` 通過，`npm test` 267/267 PASS。三個施工 commit 無各自 exact-SHA workflow run；本張交付 HEAD **CI 待驗證**。

**D1 偏差與缺口**：沿用 Registry 的 FAILED/IDLE 而非改狀態模型；`codexHomeSha256` 僅作路徑雜湊。腳本可產生兩階段與 recovery 記錄，但 `inspect-workspace` 唯讀執行函式只存在 Electron main process，未新增繞過 IPC 的純 Node 路徑，因此 Safe Bridge 兩階段可用性仍由實機操作者補證。未持有真實 OFFICIAL／PEGA 帳號或憑證，沒有任何 ENVIRONMENT PASS。此回報位於原施工單 D1 之前，D1 仍為優先規則。

## 決策紀錄 D1（2026-09-24，Claude Code 回覆 Codex 的釐清請求；**凡與上文 B 項衝突，以此為準**）

**Q1｜Worker Registry 狀態模型 → 不修訂。** `WORKER_STATES`（IDLE/RUNNING/CANCELLING/FAILED/UNKNOWN）與 Dashboard 投影綁定，屬 Blueprint 層級，本單不得更動。證據改記錄兩個既有來源：
- `registryState`：以暫存 `WorkerRegistry` 實例（放在 scratch 目錄，不碰真實 registry 檔）記錄該次指派與結果；失敗方預期 `FAILED`（或 `UNKNOWN`），健康方預期 `IDLE`（釋放後）。
- `health`：Provider Router 的健康狀態（`READY`／`AUTH_REQUIRED`／`UNAVAILABLE`／`DEGRADED`／`NOT_CONFIGURED`）。失敗方預期非 `READY` **或**其 run check 為 `FAIL`；健康方預期 `READY` 且 check 為 `PASS`。
`ACCEPTANCE.md` 第 5 節該列已同步改寫（不再要求 `UNAVAILABLE` 為 Registry 狀態）。

**Q2｜與「不得更動 CODEX_HOME」的衝突 → 禁令的精確範圍如下。**
- 禁止：修改、刪除、覆寫、複製 `auth.json`、`credentials.json` 或 `CODEX_HOME` 內任何**非 AECP 產生**的檔案；禁止把憑證寫入任何 home。
- 允許：既有模式已在做的行為——`prepareOfficial()` / `prepareCustom()` 對 AECP 管理的 `config.toml` 做**冪等**重寫（內容與正常健康流程產生者相同）。
- 故障注入方式：
  - 階段 (a)（PEGA 失敗）：OFFICIAL 與 PEGA 都用**真實 home**，走與 `multi-codex` 相同的正常 prepare；PEGA 只在**該次子程序環境**注入無效 key（`env_key` 名稱不變）。不改任何檔案。
  - 階段 (b)（OFFICIAL 失敗）：OFFICIAL 改用 **scratch 暫存 root** 下全新空的 `codex-official/codex-home`（無 auth 檔，預期 `AUTH_REQUIRED`／run `FAIL`），**完全不接觸真實 OFFICIAL home**；PEGA 用真實 home 正常執行並預期 `PASS`。
- 保護斷言（取代先前「兩個 codexHomeSha256 前後不變」——該欄位實為**路徑**的雜湊，無法證明內容未變，勿再用它當內容保護）：
  1. 階段 (b) 前後，**真實 OFFICIAL home** 內 `auth.json`／`credentials.json` 的 `{name, size, sha256(內容，僅在記憶體計算、只存摘要)}` 完全相同（該 home 在 (b) 未被使用；(a) 中它是健康方，Codex CLI 可能合法刷新 token，故 (a) 不做內容不變斷言）。
  2. PEGA home 全程 `authPresent === false`，且 `config.toml` 摘要等於正常 `prepareCustom` 的輸出摘要。
  3. 失敗方 check 的 `workerId` 等於預期 Worker，且不存在由另一 Worker 代跑（無靜默 fallback）。
  4. OFFICIAL 與 PEGA 的 `codexHome` 路徑不同。
  5. 不得把任何 auth 檔內容、key、prompt、response 寫入 evidence（僅摘要與狀態）。

**Q3｜Safe Bridge 與恢復證據 → 由本 mode 產出可自動化的部分，其餘由操作者補。**
- 階段 (c) 恢復：兩階段結束後重新對真實 OFFICIAL 與真實 PEGA 各做一次 health 檢查，兩者皆須 `READY`，寫入 `recovery` check。
- Safe Bridge 持續可用：每個故障階段之後，執行一次**唯讀** `inspect-workspace` Command Card 的純 Node 路徑（用 `electron/lib/protocol.cjs` 與現有唯讀檢視函式；若能在不啟動 Electron 下呼叫），寫成 check `safe-bridge.available`。**若現有程式碼無法在無 Electron 下呼叫，不得自行新增權限或繞過 IPC；改在回報與 runbook 中標「缺口」，該項由操作者於實機 UI 執行並以 0004 的擁有者範本提供。**
- 整體 PASS 定義不變：預期的失敗確實發生且被隔離，且 (c) 恢復成立。

**對 0003/0004 的連動：** `scripts/verify-evidence-bundle.cjs` 對 `codex-fault-isolation` 的判定改依上述欄位（`registryState`、`health`、`recovery`、保護斷言 1–5 的布林結果），不再檢查「兩個 home hash 相同/不同」以外的路徑雜湊語意。

## Claude Code 驗收（2026-09-24，HEAD `f92c0a6`）— CLOSED

獨立核對：本地與遠端同為 `f92c0a6`；該 SHA 的 Security / CI / Packaging / Build and Release 皆 success；我本機重跑 `npm run verify` exit 0。
- **A：** `verifyExpectedFile` 由腳本讀檔比對（lstat 確認為一般檔、比對內容、僅存 sha256），worker 自述不算；在隔離暫存 worktree 進行。接受。
- **B：** 完全依 D1：暫存 WorkerRegistry、`registryState` + `health`、階段 (a) 只用子程序環境注入無效 key、階段 (b) 用 scratch 空 home 且不碰真實 OFFICIAL home、auth 檔僅在記憶體算摘要且只存摘要、PEGA home 全程無 auth 檔、`noFallback` 由 `result.workerId` 核對、階段 (c) recovery。`git diff` 顯示 `electron/` 零改動，SecurityPolicy 與權限未動。接受。
- **Safe Bridge：** 正確保留 `OPERATOR_REQUIRED` 缺口，未自行繞過 IPC。→ 0005 B 補擁有者範本。
