# 施工單 0017 — 補齊最後兩條功能增強（B02-L207、B11-L61）

**簽發：** Claude Code → **執行：** Sonnet 5（施工者）｜ **日期：** 2026-09-26 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** DONE-PENDING-REVIEW
**驗收方式：** `node scripts/review-gate.cjs --base <SHA> --mode fix --ci`（協議見 `.ai/AUTONOMOUS_RUN.md`）。

## 0. 範圍與規則

這兩條不是安全缺陷，是藍圖描述的資料結構還沒做。**只做下面兩件事，不擴大範圍**，不放寬任何拒絕結果、不新增權限、不改 `Blueprint/`、`.github/`、`.ai/ACCEPTANCE.md`。每項先寫會失敗的測試（紅）→ 最小修改（綠）→ 破壞紀錄寫進 `.ai/sabotage-log.jsonl`（破壞 `electron/` 產品程式）→ 更新矩陣（`CONFIRMED_GAP` → `IMPLEMENTED`）→ 各自一個 commit。測試須在 Linux 與 Windows 都通過。

## 1. B02-L207：任務要有 role 與 verifierProfile

藍圖 02 §12：「任務是持久物件，含 dependencies、priority、**role**、Workspace／repository bindings、limits、**verifier profile** 與 risk policy；排程決定重啟後仍須可解釋」。其餘欄位已證明，缺的是 `role` 與 `verifierProfile`。
- `electron/lib/control-plane.cjs`：`enqueueTask`／任務記錄新增 `role`（預設 `builder`，限 `planner|builder|reviewer` 白名單）與 `verifierProfile`（由現有的 `task.verifier` 命令字串對應到 `npm-verify`／`npm-test`／`custom`，未知值拒絕）；兩者寫入排程決定（`schedulerDecision`）並跨重啟保存。
- 既有行為不變：沒指定時結果與現在完全相同（預設值），所有既有測試不得改斷言。
- 測試：真 ControlPlane、重啟後兩欄位與排程決定仍在；非法 role／profile 被拒；預設值不改變既有派發結果。

## 2. B11-L61：動作 metadata 表

藍圖 11 §3：「每個動作必須有專屬的 schema、有界輸入、風險分級與 verifier；通用的自由字串 shell 不是偏好的公開契約」。
- `electron/lib/protocol.cjs`：把 `ACTION_TYPES` 改成表（每個動作：`schema`（允許欄位與型別）、`risk`（`GREEN|YELLOW|RED`）、`verifier`、`maxInputBytes`）；`parseCommandCard` 與 `task:import`（`electron/main.cjs`）**從這張表讀取**驗證與風險，不再各自寫死。
- 現有兩個唯讀動作（`inspect-workspace`、`git-status`）行為不變；自由 shell 字串、多餘欄位、自行提升風險仍被丟棄／拒絕（既有 `tests/task-bridge-main.test.cjs` 必須照過）。
- 測試：表中每個動作都有 schema／risk／verifier；新增一個假動作到表中可被 import 讀到（證明是表驅動）；不在表中的動作被拒；risk 由表決定，卡片自報的 risk 被忽略。

## 3. 完成時

`npm run verify` 全綠、閘門 ACCEPTED（含 CI）、`.ai/STATUS.md` 頂部結論更新統計、本檔追加回報並改 `DONE-PENDING-REVIEW`。

## 回報

### 完成（紅測試先行）
**B11-L61**：`protocol.cjs` 新增 `ACTION_TABLE`（每個動作的 schema、risk、verifier、maxInputBytes）與 `actionMeta()`；`parseCommandCard` 依表保留欄位、拒絕型別錯誤與超量輸入、以表的 verifier 產生 verification；`main.cjs` 的 `task:import` 由表取得 risk（不再寫死 GREEN），卡片自報的 risk／verifier 一律忽略。`ACTION_TYPES` 仍是同內容的陣列（既有斷言不變）。兩個現有動作行為不變。測試：`tests/action-metadata-table.test.cjs`（含在表中加入假動作證明表驅動）。

**B02-L207**：`control-plane.cjs` 的 `enqueueTask` 新增 `role`（預設 builder，白名單 planner|builder|reviewer）與 `verifierProfile`（由 `verifier` 命令字串對應 npm-verify／npm-test／custom，或明確指定；未知值拒絕且不排入佇列），兩者寫入 `aecp.scheduler-decision/v1` 並跨重啟保存；舊任務缺欄位時以預設值說明。規劃器輸出的 `verifier` 字串現在會傳入。測試：`tests/task-role-verifier.test.cjs`。

兩條矩陣項目 CONFIRMED_GAP → IMPLEMENTED；破壞紀錄 13 筆（其中一筆一開始未被偵測：兩個動作的 verifier 值相同，補了「表中 verifier 為 file-exists 的假動作」測試後 DETECTED）。矩陣：358 已實作、21 部分、0 確認缺失、16 人工、18 環境、14 擁有者、14 蓄意不做。

