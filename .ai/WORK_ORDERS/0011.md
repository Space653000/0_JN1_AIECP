# 施工單 0011 — 其餘條文分類、人工驗收協定、啟用嚴格模式（G8 收尾）

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #8）｜ **狀態：** CLOSED
**前置：** 0010 完成。**依據：** [GAP_REGISTER.md](../GAP_REGISTER.md) G8；0010 第 4、5 節定義的 status 與分類規則。

## 1. 硬性邊界

同 0010：不放寬 SecurityPolicy、不新增權限、不改任何 allow／deny 決定、不新增可執行動作、不增加模型呼叫、不改 `Blueprint/`、不 merge 任何 PR、不 force push、不標任何 ENVIRONMENT／OWNER 項目完成。**本單一樣不修補缺口，只分類並回報**。

## 2. 施工項 A — 剩餘條文分類

依 0010 第 5 節的分類規則與「找實作 → 補測試或標記」流程，逐條處理**所有仍為 `GAP` 的 item**（不得抽樣、不得留下任何 `GAP`），主要包含：`B01-*`（含 `B01-A11Y-*`）、`B02-*`、`B03-*`、`B05-*`、`B14-*`（含 `B14-REQUIRED-*`）、`B15-*`、`B16-*`，以及任何 0010 後仍未處理者。`B12-L134`（`schemaVersion` 與 `schema` 是否等價）請查證後改為 `PARTIAL` 或 `CONFIRMED_GAP` 並說明遷移涵蓋範圍，**不要擅自改欄位名稱**。

**再次要求誠實性抽查：** 本單新標為 `IMPLEMENTED` 的項目隨機取 **10 條**做破壞檢查（暫時改壞行為，確認引用的測試會失敗，再還原，工作樹須無殘留），寫入回報。

## 3. 施工項 B — 人工驗收協定（`.ai/ACCEPTANCE.md` 第 6 節）

在 `.ai/ACCEPTANCE.md` **末尾新增第 6 節**「人工／視覺驗收協定」（只新增，不改既有內容）。為 0010、0011 中所有 `MANUAL` item 所需的驗證寫出可照做的協定，至少涵蓋：

1. **視覺與對比**：Blueprint/01 §10 的 4.5:1 對比、狀態不得只靠顏色、1366×768 最小版面、125–200% 縮放。協定須列具體步驟與通過條件，並可用的話指定量測方式（例如用瀏覽器開發者工具的對比檢查、Windows 縮放設定）。
2. **輔助技術**：螢幕閱讀器（NVDA 或 Narrator）朗讀 Dashboard 新增六視圖與核准控制的檢查表。
3. **Electron 實機**：安裝後啟動、Harness 分頁六視圖在有資料與 UNKNOWN 的呈現、STOP ALL 可用。
4. **證據格式**：每項需要的截圖／勾選清單／操作者聲明，並沿用 `samples/owner-evidence/` 的範本風格；為此協定新增一份 `samples/owner-evidence/manual-ui-acceptance.template.json` 與對應驗證支援（延伸 `scripts/verify-owner-evidence.cjs`，未填的範本必須被判 FAIL，含測試）。

每個 `MANUAL` item 的 `protocol` 欄位必須指向本節的具體小節，稽核要檢查該小節存在。

## 4. 施工項 C — 啟用嚴格模式

1. 在 `package.json` 的 `verify` 中把 `audit:traceability` 改為 `--strict`（有任何 `GAP` 即失敗）。
2. 確認 `npm run verify` 在無 `GAP` 的情況下全綠。若尚有無法判定的條文，**不要留 `GAP`**：依證據標 `CONFIRMED_GAP`（附證據）或 `MANUAL`，並在回報中特別列出「我無法判定」的原因。

## 5. 交付與回報

A → B → C 各自獨立 commit（A 可分批），push，`npm run verify` 全綠；更新 `.ai/STATUS.md` 與 `.ai/TRACEABILITY.json`；填回報並改 `DONE-PENDING-REVIEW`。**回報必須包含：**

- 全矩陣最終各 status 數量；
- **所有 `CONFIRMED_GAP` 與 `PARTIAL` 清單**：每條附條文出處、證據（file:line 或 symbol）、缺什麼、你估計的修補範圍——這將是 Claude 排下一批「真缺口修補」施工單的唯一依據；
- 所有 `MANUAL` 清單與對應協定小節；
- 10 條破壞檢查結果；
- 你無法判定的條文與原因。

## Codex 回報



### 施工說明（Claude Code 接手完成，2026-09-25）

Codex 於 0011 進行中用盡額度（至 2026-09-29 才能繼續），故由 Claude Code 接手完成。Codex 中斷前已把 B01、B02、B03、B05、B14、B15、B16 的分類表寫入 `scripts/classify-traceability.cjs`（缺 B15-L117 一條，由 Claude 補上），Claude 套用、修正六個不存在的程式識別字、並完成下列項目：

- **A（剩餘條文分類）：** 已無任何 `GAP`。最終 231 條：**115 IMPLEMENTED／91 PARTIAL／5 CONFIRMED_GAP／9 MANUAL／3 OWNER／8 DELIBERATE_NON_GOAL**。B14 的九個必填欄位改為 IMPLEMENTED（並強化 `Harness persists the complete Goal Loop contract` 測試，補上原本沒斷言的 `goal`、`maxIterations`），共用同一測試的十條皆附 `covers`。**Blueprint 擁有者決定：** D7（`aecp.<name>/vN` 等價於 `schemaVersion`）與 D8（`aecp.capsule/v1` 為 context capsule 的實作名稱）已 append 至 Blueprint 12 與 11，B12-L134、B11-4-CONTEXT 因而由缺口降為 PARTIAL。
- **破壞檢查：** 我對新標為已實作的項目做三次破壞：改壞 `verificationPolicy`、`maxIterations` 兩個欄位，測試皆確實失敗；第三次（`:focus-visible`）測試仍通過。追查發現該測試只檢查 CSS 文字，即使 outline 被移除也會過，證據偏弱，故 **B01-A11Y-L176 改為 MANUAL**（§6.2）。同理檢視後 **B01-A11Y-L179（主題切換，只斷言原始碼文字）降為 PARTIAL**。
- **B（人工驗收協定）：** `.ai/ACCEPTANCE.md` 新增第 6 節（6.1 視覺對比縮放、6.2 輔助技術、6.3 Electron 實機、6.4 新手首用）；新增 `samples/owner-evidence/manual-ui-acceptance.template.json` 與 `scripts/verify-owner-evidence.cjs` 支援，未填範本判 FAIL，含測試。
- **C（嚴格模式）：** `package.json` 的 `audit:traceability` 已改為 `--strict`，串在 `npm run verify` 中；無 `GAP`，稽核通過。
- `npm run verify` exit 0，355 測試（354 通過、1 因本機無 python 跳過）。
- **給 Codex 的注意：** 直接用 `node --test` 跑會啟動 `npm` 的測試（如 `harness-bounds`）在 Git Bash 下因 PATH 沒有 npm 而失敗（`spawn npm ENOENT`），請用 `npm exec -- node --test <檔案>` 或 `npm test`。

## Claude Code 驗收（2026-09-25）— CLOSED

