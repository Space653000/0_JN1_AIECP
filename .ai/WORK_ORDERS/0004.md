# 施工單 0004 — 擁有者閘門手冊與實機驗收記錄範本

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** OPEN
**前置：** 0003 完成後接續施工。

## 0. 目的

剩下的 ENVIRONMENT / OWNER-EXTERNAL 閘門只有擁有者能在真實環境完成。本單讓擁有者「照表操作、貼回結構化記錄」即可，Claude 才能機械驗收。**本單只產出文件、範本與驗證邏輯，不得宣稱任何閘門完成。**

## 1. 硬性邊界

同 0002/0003。範本與文件不得含真實憑證、帳號、憑證私鑰、內部 URL；範例值一律標示為 `<placeholder>`。

## 2. 施工項 A — 實機驗收記錄範本與驗證

`.ai/ACCEPTANCE.md` 第 5 節列了四類需實機/擁有者證據的項目：實體 ARM64 UI smoke、使用者筆電 CLI/verifier 安全、Official Full MCP、（故障隔離已由 0002 涵蓋，此處不重做）。為前三者各新增 JSON 範本於 `samples/owner-evidence/`：`arm64-ui-smoke.template.json`、`laptop-verifier-safety.template.json`、`official-full-mcp.template.json`。每份含：`schema`（如 `aecp.owner-evidence.arm64-ui-smoke/v1`）、`sourceCommit`、`date`、`machine`（含 `arch`）、`operatorAttestation`、清單欄位（逐項 boolean + 備註，項目必須與 ACCEPTANCE 第 5 節表格逐項一致）、`artifacts`（雜湊/檔名，不含內容）。

擴充 0003 的 `scripts/verify-evidence-bundle.cjs`（或新增姊妹腳本 `scripts/verify-owner-evidence.cjs`，擇一並說明）以驗證上述三種記錄：schema 正確、SHA 相符、清單全部為 true 才 PASS、缺欄/佔位符 `<placeholder>` 未替換 → FAIL、機密掃描同 0003。範本本身（未填）必須被驗證器判為 FAIL，以證明不會被誤當成證據。

驗收（TESTED）：新增測試覆蓋填妥 PASS、任一清單 false FAIL、佔位符殘留 FAIL、SHA 不符 FAIL、含機密樣式 FAIL；`npm run verify` 全綠。

## 3. 施工項 B — `Reports/OWNER_GATES_RUNBOOK.md`

針對 STATUS.md 的 OWNER-EXTERNAL 四類（Authenticode 簽章、Microsoft Store/Partner Center、公開/區網遠端閘道、正式生產供應商宣稱）各一節。**必須從實際檔案推導**：先讀 `.github/workflows/signed-release.yml`、`.github/workflows/store-package.yml`、`release/store/README.md`、`release/store/store-metadata.template.json`、`scripts/validate-store-package.ps1`、`electron/lib/authenticode.cjs`、`electron/lib/remote-gateway.cjs`、`electron/lib/pairing.cjs`、`Blueprint/07_INSTALL_RELEASE.md`、`Blueprint/16_*.md`。每節含：擁有者需準備什麼（憑證/身分/網域，**只說明所需，絕不索取或寫入密鑰**）、需設定的 repo secrets 名稱（以 workflow 實際引用為準）、觸發步驟（workflow 名稱與 input）、成功產物與驗證方式（例如 SHA-256、`requiredSignerThumbprint`）、失敗回退、以及**現有腳本無法覆蓋的缺口**（標「缺口」，不得虛構步驟）。

驗收（STATIC + Claude Review）：四類皆有對應小節；引用之檔案路徑/secret 名稱/workflow input 皆真實存在，Codex 於回報附上 grep 核對清單。

## 4. 施工項 C — Blueprint 23 與 `.ai/STATUS.md` 對齊

`Blueprint/23_IMPLEMENTATION_STATUS.md` 是原始權威快照，`.ai/STATUS.md` 是整併版，兩者須一致。用 **append-only** 方式在 23 號文件末尾新增一節「2026-09-24 施工單 0001–0004 更新」，摘要：run() 收斂測試、README 歷史封存、環境 runbook、驗收協定、證據驗證器、ARM64 標籤、擁有者範本與手冊、PR #7 合併就緒文件；並重申所有 ENVIRONMENT / OWNER-EXTERNAL 仍未完成。**不得改寫或刪除 23 號既有內容**，不得改動 Blueprint 其他編號。跑 `npm run verify`（`audit:blueprints` 等會檢查 Blueprint）確認全綠。

## 5. 交付要求

A → B → C 各自獨立 commit（英文簡短訊息），push；`npm run verify` 全綠；更新 `.ai/STATUS.md`；填「Codex 回報」改狀態 `DONE-PENDING-REVIEW`。最後回報須列出：GitHub 遠端 HEAD SHA、本地 HEAD SHA（必須相同）、`git status` 乾淨、該 SHA 四組 workflow 結果。

## Codex 回報

（尚未填寫）
