# 施工單 0005 — 證據來源標示修正與故障期 Safe Bridge 範本

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** OPEN

## 0. 背景

0002–0004 已驗收 CLOSED，本單收尾 Claude 審查發現的兩項缺口（詳見 0003、0002 驗收紀錄）。範圍小、以修正為主。**硬性邊界同 0002–0004**：不改 `electron/`、不放寬 SecurityPolicy、不新增權限、不改 Blueprint 00–24、不標任何 ENVIRONMENT / OWNER 項目完成、不 merge PR #7、不 force push、證據不得含憑證/prompt/response。

## 1. 施工項 A — 修正證據來源標示（缺陷 F1）

現況：`scripts/verify-evidence-bundle.cjs` 依賴 `evidence.workflowProvenance`，但沒有任何程式碼寫入它，導致標示永遠是 `LOCAL_SCRIPT`。

1. `scripts/provider-environment-verify.cjs`：僅當環境變數 `GITHUB_ACTIONS === 'true'` 時，於 evidence 頂層寫入 `workflowRun: { repository, workflow, runId, runAttempt, sha }`（分別來自 `GITHUB_REPOSITORY`、`GITHUB_WORKFLOW`、`GITHUB_RUN_ID`、`GITHUB_RUN_ATTEMPT`、`GITHUB_SHA`）；本機執行不得寫入此欄位。這些都是非機密識別資訊，不得寫入任何 token（不要整包倒環境變數）。
2. `verify-evidence-bundle.cjs`：移除 `workflowProvenance`。改為：有 `workflowRun` 且欄位格式合法 → 輸出 `PROVENANCE: WORKFLOW_CLAIMED`，並印出**可直接複製的核對指令** `gh run view <runId> --repo <repository> --json headSha,conclusion,workflowName`，說明「須人工/Claude 核對輸出的 headSha 等於 evidence.sourceCommit 且 conclusion 為 success，驗證器本身不聯網」；無 `workflowRun` → 維持 `LOCAL_SCRIPT（非 workflow PASS）`。另外，`workflowRun.sha` 存在且不等於 `sourceCommit` → FAIL（欄位矛盾）。此標示不得單獨改變其他檢查的 PASS/FAIL 語意（除上述矛盾檢查外）。
3. 同步 `tests/verify-evidence-bundle.test.cjs`（涵蓋：無 workflowRun → LOCAL_SCRIPT；合法 workflowRun → WORKFLOW_CLAIMED 且輸出含 gh 指令；workflowRun.sha 與 sourceCommit 不符 → FAIL；`workflowRun` 含疑似機密 → 既有機密掃描 FAIL）、`tests/provider-environment-workflow.test.cjs` 或對應測試（涵蓋 script 在 `GITHUB_ACTIONS=true` 與非 Actions 環境下的欄位有無）。
4. 更新 `Reports/ENVIRONMENT_EVIDENCE_RUNBOOK.md` 共通規則：說明兩種標示的意義與核對指令。

驗收（TESTED）：上述測試通過；`npm run verify` 全綠。

## 2. 施工項 B — 故障期 Safe Bridge 擁有者範本

`ACCEPTANCE.md` 第 5 節「故障隔離」要求故障期間 Safe Bridge 可用，但 `codex-fault-isolation` 對此只能標 `OPERATOR_REQUIRED`（無 Electron 無法自動檢查）。補齊可機械驗收的路徑：

1. 新增 `samples/owner-evidence/safe-bridge-during-fault.template.json`，schema `aecp.owner-evidence.safe-bridge-during-fault/v1`，沿用其他三份範本的欄位結構（`sourceCommit`、`date`、`machine`、`operatorAttestation`、`checklist`、`artifacts`），checklist 項目：`safeBridgeAvailableDuringPegaFailure`、`safeBridgeAvailableDuringOfficialFailure`、`inspectWorkspaceCardPassedDuringFault`、`bothWorkersRecoveredReady`；artifacts 至少含對應的 provider evidence JSON（`fileName` + `sha256`），且驗證器須要求該 sha256 為 64 碼 hex。
2. `scripts/verify-owner-evidence.cjs` 新增此 schema 的支援（沿用 `TYPES` 與 artifacts 規則）；範本本身必須被判 FAIL（佔位符）。
3. 測試：填妥 PASS、任一項 false FAIL、佔位符殘留 FAIL、SHA 不符 FAIL、含機密樣式 FAIL。
4. 更新 `Reports/ENVIRONMENT_EVIDENCE_RUNBOOK.md` 第 10 節：說明 Safe Bridge 部分改由此範本補證，並指示同時提交該次 `codex-fault-isolation` 的 evidence JSON 與此範本；更新 `.ai/ACCEPTANCE.md` 第 5 節故障隔離該列末尾加一句「Safe Bridge 部分以 `safe-bridge-during-fault` 範本補證」。

驗收（TESTED + STATIC）：測試通過；`npm run verify` 全綠。

## 3. 施工項 C — 合併後首次派發檢查清單

在 `Reports/PR7_MERGE_READINESS.md` 末尾**新增**（不改既有內容）「合併後首次派發驗證」小節，說明：`provider-environment.yml` 的動態 `runs-on` 只有合併到 `main` 後首次派發才會被 GitHub 實際解析；操作者第一次應在**不需要真實憑證的低風險路徑**派發驗證解析是否成功（例如 `mode` 選需最少前置者，並讓它在前置檢查步驟即因缺條件而安全失敗，重點只確認 workflow 有被排入並選到正確標籤，不是取得 provider 證據），失敗時如何回報。指令與 input 名稱須以檔案實際內容為準。

## 4. 交付要求

A → B → C 各自獨立 commit（英文簡短訊息），push；`npm run verify` 全綠；更新 `.ai/STATUS.md`；填「Codex 回報」並改狀態 `DONE-PENDING-REVIEW`；最終回報含本地/遠端 HEAD SHA（須相同）、`git status`、該 HEAD 四組 workflow 結果與連結、缺口與偏差。

## Codex 回報

（尚未填寫）
