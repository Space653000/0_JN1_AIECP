# 施工單 0001

**簽發：** Claude Code（Blueprint / Review / 驗收） → **執行：** Codex（Builder）
**日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** DONE-PENDING-REVIEW

## 0. 前次施工 Review 結果（HEAD `9c232a9`）

| 項目 | 結論 |
|---|---|
| `AGENTS.md` / `.ai/CODEX_WORKER.md` | 接受。**缺口：** 未規定要先讀施工單 → 本次由 Claude 補上（見第 5 節） |
| `provider-router.cjs` `run()` 程序收斂修正 | 接受（TESTED + CI）。邏輯正確：exit 後 500ms 寬限、timeout/abort/超量後 2s 強制收斂、pipe 被孫程序占住時銷毀。 |
| 證據等級核對 | 本機 `npm run verify` 與 `9c232a9` 四組 workflow（Security / CI / Packaging / Build and Release）皆 SUCCESS，已由 Claude 以 `gh run list` 獨立核對。等級 = **CI**，未越級宣稱。 |
| **意見 R1（非阻擋）** | 新測試只覆蓋「exit 但無 close」路徑；`scheduleForceSettle` 的 timeout / abort / output-limit 三條路徑**沒有測試** → 施工項 A |
| **意見 R2（非阻擋）** | README 仍有多段按日期堆疊的狀態區塊（STATUS.md 已標為雜訊）→ 施工項 B |

## 1. 本次範圍與硬性邊界

**判斷：** STATUS.md 的 ENVIRONMENT / OWNER-EXTERNAL 項目**不是 Codex 能靠寫程式關閉的**。本單只處理倉庫內可施工項，並替 ENVIRONMENT 閘門備妥「操作者照著做即可產出證據」的 runbook。

**禁止事項：**
- 不得把任何 ENVIRONMENT / OWNER-EXTERNAL 項目標為完成，不得偽造 provider 證據。
- 不得放寬 SecurityPolicy、不得新增權限、不得改動 Blueprint 00–24 既有內容（僅可依第 3 項 B 搬移 README 內容）。
- 不得 merge PR #7、不得 force push、不得刪除任何歷史文件內容（搬移須逐字保留）。

## 2. 施工項 A — 補齊 `run()` 收斂路徑測試（回應 R1）

檔案：`tests/provider-router.e2e.test.cjs`（必要時可另立測試檔）。沿用現有 `spawnImpl` 注入 + fake child 手法，新增測試：

1. **timeout**：fake child 永不 exit/close，`timeoutMs` 最小值觸發 → `timedOut === true`、約 2s 內 resolve、stdout/stderr 已 destroy。
2. **abort**：`AbortController.abort()` → `aborted === true`、同樣在強制收斂時限內 resolve。
3. **output limit**：輸出超過 `maxOutputBytes` → `outputLimitExceeded === true`、resolve、不再累積輸出。
4. **exit 後寬限內仍到達的資料被保留**：exit 後 <500ms 內再寫入 stdout，結果須包含該資料。
5. **不重複 settle**：`close` 在 force-settle 之後才到達，不得二次 resolve / 拋錯。

驗收（TESTED）：5 個新測試通過；不得使用真實 sleep 超過 3 秒/測試；測試不得依賴外部網路或憑證。

## 3. 施工項 B — README 狀態區塊整併（回應 R2）

1. 把 README 內按日期附加的狀態區塊（2026-09-19 / 09-22 / 09-23 等）**逐字**搬到新檔 `Reports/README_STATUS_HISTORY.md`（檔頭註明來源與搬移日期）。
2. README 只保留**一個**簡短「Current status」小節：指向 `.ai/STATUS.md` 與 `Blueprint/23_IMPLEMENTATION_STATUS.md` 為權威來源，並保留版本說明（`v0.3.0` 套件版本 vs V3.0 Multi-Worker 藍圖階段）與使用者安裝/使用說明。
3. **陷阱：** `scripts/acceptance-audit.cjs`、`scripts/blueprint-coverage.cjs`、`scripts/roadmap-gate-audit.cjs`、`tests/release-workflow.test.cjs` 可能對 README 文字做斷言。先 `grep` 這些檔案確認被檢查的字串；若某斷言依賴被搬走的內容，須把該必要語句保留在 README 或同步調整斷言並在回報中說明原因（不得為了通過而刪除斷言）。

驗收（TESTED + CI）：`npm run verify` 全綠；README 行數明顯下降；搬移前後的被搬內容 diff 為逐字相同。

## 4. 施工項 C — ENVIRONMENT 證據 Runbook

新增 `Reports/ENVIRONMENT_EVIDENCE_RUNBOOK.md`，供 repo 擁有者在真實機器上操作。**內容必須從實際檔案推導，不得憑空編造指令**：先讀 `.github/workflows/provider-environment.yml`、`scripts/provider-environment-verify.cjs`、`Blueprint/24_LOCAL_MODEL_PROVIDER_VERIFICATION.md`、`Blueprint/03_PROVIDER_ROUTER.md`、`electron/lib/codex-worker-runtime.cjs`、`electron/lib/pega-provider.cjs`。

須涵蓋 `.ai/STATUS.md`「環境閘門」中每一項（逐項一小節），每節包含：
- 前置條件（機器、帳號、憑證由誰提供；**憑證只以環境變數/credentialRef，絕不寫入 repo**）
- 精確操作步驟（workflow 名稱與 input、如 `expected_arch: any | x64 | arm64`；或本機指令）
- 成功時產出的證據檔/欄位（須對應 ACCEPTANCE 第 0 節：架構、worker 身分、CODEX_HOME hash、模型、健康度、verifier 結果，且不含憑證/prompt/response 內容）
- 如何把證據回報給 Claude Code 驗收（貼哪些欄位、exact-source commit SHA）
- 該項若**無法**由現有 workflow/script 產生證據，明確標「缺口」而非虛構步驟，並在回報列出缺口清單

驗收（STATIC + Claude Review）：每個 ENVIRONMENT 項都有對應小節；所有引用的檔案路徑、workflow 名稱、input 名稱皆真實存在（Codex 自行 grep 核對並在回報附上核對清單）。

## 5. 流程補強（Claude 已修改，Codex 無需動手）

Claude 已在 `AGENTS.md` 與 `.ai/CODEX_WORKER.md` 加入「先讀 `.ai/WORK_ORDERS/` 中最新 OPEN 施工單」的步驟。之後施工單編號遞增（0002…），Codex 完成後在該單末尾的「Codex 回報」小節填寫並把狀態改為 `DONE-PENDING-REVIEW`，由 Claude 驗收後改為 `CLOSED`。

## 6. 交付要求

1. 依 A → B → C 順序，各項獨立 commit（英文簡短訊息），全部 push 到本分支。
2. 更新 `.ai/STATUS.md`：施工項完成度、測試數、各 commit SHA 與對應 workflow 結果（須為該 SHA 的實際結果）。Claude 對上一輪的 Review 結論不需重寫。
3. 在本檔下方「Codex 回報」填寫：每項的 commit SHA、`npm run verify` 結果、遇到的偏差/缺口、需要 Claude 或使用者決策的問題。
4. GitHub Actions 未跑完時，回報為「CI 待驗證」。

## Codex 回報

### 施工結果（2026-09-24）

| 項目 | Commit | 結果 |
|---|---|---|
| A | `017dca4df28d20109e322924c44460531d301669` | 5 個新增 `run()` fake-child 測試通過；涵蓋 timeout、abort、output limit、exit 後寬限內資料、late close 不重複 settle。無外部網路或憑證。 |
| B | `dd54cdf0e7405acb5c92e84d13eec520b6bffd06` | README 901 → 680 行，兩段日期狀態移至 `Reports/README_STATUS_HISTORY.md`；搬移內容以原 Git blob 逐字比對通過（僅換行格式正規化）。所列四個 audit/test 檔已檢查，沒有依賴被搬文字的斷言，不需修改斷言。 |
| C | `86236b44deedfcd2a25eb287fcb569ead46aec9b` | `Reports/ENVIRONMENT_EVIDENCE_RUNBOOK.md` 對應全部 10 項 ENVIRONMENT 待辦，含前置條件、操作、證據、回報與無法現行產證的缺口。未把任何環境項目標為完成。 |

**驗證：** 施工前、B 後與 C 後 `npm run verify` 均通過；C 後 `npm test` 259/259 PASS，六項 audit/check 流程皆無失敗。A 專項 `node --test tests/provider-router.e2e.test.cjs` 7/7 PASS。A/B/C 施工 commit 沒有各自 exact-SHA GitHub workflow run，不得沿用 `9c232a9` 或 `268f01d` 的結果。

**整批交付 CI：** A/B/C commit 隨同批推送，沒有個別 exact-SHA run；交付 SHA `34e262d3bd455788a86445cee59ad221a8282873` 的 [AECP Security #35967550698](https://github.com/Space653000/0_JN1_AIECP/actions/runs/35967550698)、[AECP CI #35967550747](https://github.com/Space653000/0_JN1_AIECP/actions/runs/35967550747)、[CI/Packaging #35967550767](https://github.com/Space653000/0_JN1_AIECP/actions/runs/35967550767)、[Build and Release #35967550886](https://github.com/Space653000/0_JN1_AIECP/actions/runs/35967550886) 皆已核對為 **SUCCESS**。這不升級任何 ENVIRONMENT／OWNER-EXTERNAL 項目的證據等級；補記 CI 的文件 commit 本身須另行驗證。

**C 靜態核對清單：** 已逐一讀取且路徑存在：`.github/workflows/provider-environment.yml`、`scripts/provider-environment-verify.cjs`、`Blueprint/24_LOCAL_MODEL_PROVIDER_VERIFICATION.md`、`Blueprint/03_PROVIDER_ROUTER.md`、`electron/lib/codex-worker-runtime.cjs`、`electron/lib/pega-provider.cjs`。workflow 實際名稱 `AECP Real Provider Evidence`；inputs 為 `source_ref`、`mode`、`model`、`local_command`、`local_args_json`、`official_model`、`pega_model`、`pega_wire_api`、`codex_worker_root`、`expected_arch`；mode 八種及架構 `any/x64/arm64` 均與檔案核對。script 的對應 `AECP_PROVIDER_VERIFY_*` 環境變數、十項所述 check id、JSON 欄位也已對照實作；`electron/mcp-server.mjs`、`.ai/STATUS.md`、`.ai/ACCEPTANCE.md` 引用路徑存在。

**偏差／缺口：** GitHub 目前未於預設分支列出 `provider-environment.yml`，查 `gh run list --workflow provider-environment.yml` 回 404；不可在未 merge PR #7 前宣稱可派發或取得真實環境 workflow 證據。手冊列出在目標 checkout 手動執行底層 script 的替代路徑，但它不等於 GitHub workflow PASS。OFFICIAL/PEGA 單 Worker、local-command 無獨立檔案 verifier；ARM64 runner 無專屬標籤；實體 ARM64 UI、筆電 verifier 安全審查、Official Full MCP、真實供應商故障隔離仍需擁有者/Claude 決定驗收協定與提供環境。未修改 Blueprint 00–24、SecurityPolicy、權限，未 merge PR #7、未 force push、未刪除歷史內容。

**請 Claude Code 決策／驗收：** Review A/B/C 與交付 HEAD 的 exact-SHA CI；決定何時使環境 workflow 在預設分支可派發，以及上述無現成證據路徑項目的驗收協定。ENVIRONMENT、OWNER-EXTERNAL 均保持未完成。
