# 施工單 0003 — 證據驗證器與 ARM64 runner 標籤

**簽發：** Claude Code → **執行：** Codex ｜ **日期：** 2026-09-24 ｜ **分支：** `feat/control-plane-complete-loop`（PR #7）｜ **狀態：** CLOSED
**前置：** 0002 完成後接續施工（依編號順序）。

## 0. 目的

讓 Claude Code / 擁有者驗收 ENVIRONMENT 證據時「機械化、不靠肉眼」：提供一支驗證器，把 `.ai/ACCEPTANCE.md` 第 0 與第 5 節的判定條件變成可執行檢查；並補上 ARM64 runner 專屬標籤缺口。

## 1. 硬性邊界

同 0002：不放寬 SecurityPolicy、不新增權限、不標任何 ENVIRONMENT / OWNER 項目完成、不 merge PR #7、不 force push、不改 Blueprint 00–24、evidence 不得含憑證/prompt/response。驗證器**只讀**，不得聯網、不得寫入被驗證的證據檔。

## 2. 施工項 A — `scripts/verify-evidence-bundle.cjs`

輸入：一個或多個 evidence JSON 路徑，加上 `--expect-sha <40碼>`、`--expect-mode <mode>`、`--expect-arch any|x64|arm64`（皆可選，但至少 SHA 必填）。檢查（每項輸出 PASS/FAIL 與原因，任一 FAIL → exit code 1）：
1. `schema === 'aecp.provider-environment-evidence/v1'`。
2. `sourceCommit` 為 40 碼且等於 `--expect-sha`。
3. `mode`、`arch`、`expectedArch` 與期望一致；`expectedArch !== 'any'` 時 `arch === expectedArch`。
4. `summary.failed === 0`（`codex-fault-isolation` mode 依 0002 B 的定義處理「預期失敗」）；每個 `checks[].status` 為合法值。
5. `privacy` 三個布林值皆為 false；並對整份 JSON 做**機密掃描**（可重用 `electron/lib/redaction.cjs`），發現疑似 token/key/Bearer/私鑰樣式 → FAIL（回報位置但不回印內容）。
6. 對 `codex-official` / `codex-pega` / `multi-codex` 相關 check：必須含 `workerId`、`model`、`health`、`codexHomeSha256`；`multi-codex` 兩個 `codexHomeSha256` 不得相同；有 `fileVerifier`（見 0002 A）者須 `expectedSha256Match === true`。
7. 標示證據來源：JSON 內無 workflow provenance 時輸出 `PROVENANCE: LOCAL_SCRIPT（非 workflow PASS）`，有則輸出 `WORKFLOW`。此欄僅供人閱讀，不改變 PASS/FAIL。

驗收（TESTED）：`tests/verify-evidence-bundle.test.cjs` 涵蓋：合法證據 PASS；SHA 不符、arch 不符、`summary.failed>0`、privacy 為 true、含偽造 `sk-`/Bearer 字串、兩個 home hash 相同、fileVerifier 不符 — 各自 FAIL。加入 `npm run check` 的語法檢查與 `npm test`；文件於 runbook「回報」段落補上「回報前先自行執行此驗證器」的指令。

## 3. 施工項 B — ARM64 專屬 runner 標籤

`.github/workflows/provider-environment.yml` 目前 `runs-on: [self-hosted, Windows, aecp-provider]`，架構閘門只能事後拒絕錯配。改為可依 `expected_arch` 選擇 runner：`x64` → 額外要求標籤 `X64`，`arm64` → 額外要求標籤 `ARM64`，`any` → 維持原標籤。實作需用 GitHub Actions 支援的方式（例如 `fromJSON` 動態 `runs-on`，或拆成 job matrix/條件 job）；**先確認語法在 Actions 中合法**，並保留既有 `expected_arch` 執行期 hard gate 不得移除。同步 `tests/provider-environment-workflow.test.cjs` 的斷言與 runbook 缺口 3 的敘述。

驗收（TESTED + CI）：workflow 測試通過；`npm run verify` 全綠；`actionlint` 若不可用，至少以 YAML 解析測試證明結構合法，並在回報說明未能用 actionlint 的原因。

## 4. 施工項 C — PR #7 合併就緒檢查（文件，不合併）

新增 `Reports/PR7_MERGE_READINESS.md`：以**當前 HEAD** 的事實列出：分支相對 `main` 的 commit 數與變更檔案數、四組 workflow 的 exact-SHA 結果（附連結）、`npm run verify` 結果、工作區乾淨與遠端同步、`main` 缺少而 PR 帶入的 workflow 清單（特別是 `provider-environment.yml` 合併後才可派發）、風險與回退方式（僅描述 `git revert -m 1 <merge-sha>`，不得執行）、合併後應立即執行的驗證清單。文件末尾明確寫「合併為人工閘門，由 repo 擁有者執行」。所有數字必須用指令實測，不得沿用舊數字。

## 5. 交付要求

A → B → C 各自獨立 commit（英文簡短訊息），push；`npm run verify` 全綠；更新 `.ai/STATUS.md`；填「Codex 回報」並改狀態 `DONE-PENDING-REVIEW`；GitHub Actions 未完標「CI 待驗證」。遇到需 Blueprint 決策或新增權限，停下來寫在回報中。

## Codex 回報

**Codex 施工結果（2026-09-24）**：A `6c8ef5560f7095503940ef1cc339a717d9034d39`（唯讀驗證器，10 個新測試），B `a65e5a2c5d897c10c977f56e38beca1f0f4bfead`（動態 X64/ARM64 runner 標籤；官方 Actions `runs-on` 表達式文件與 `js-yaml` parse test 核對），B 補正 `119a77c4190cf3307935f2af0db6786b1d98e2f6`（保留 acceptance audit 對 dedicated runner 的斷言、改查動態標籤），C `5521ab47bc231ba48c6cdaeba502a317f1d1f0cf`（PR #7 合併就緒快照）。最初 B 提交在 audit 中因舊固定 `runs-on` 正則失敗，補正後 `npm run verify` 全綠、`npm test` 278/278 PASS。A/B/C 沒有冒用其他 SHA 的 CI；最終交付 HEAD 的四組 workflow **CI 待驗證**。

**偏差／缺口**：本機查無 `actionlint`，以 repo 已安裝的 `js-yaml` 解析 workflow，並依 [GitHub 官方 runs-on 語法](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#jobsjob_idruns-on) 確認 `fromJSON` 與 labels 陣列路徑；此測試不等於實際 self-hosted runner 排程。擁有者仍須提供並標記 X64/ARM64 runner。PR 就緒文件以提交前 `119a77c…` 的實測 HEAD 作時間點快照；文件及本回報後的新 HEAD 必須重新核對 CI 與比較數字，尚未合併 PR #7。`provider-environment.yml` 仍不在 `main`，派發真實環境證據需等人工合併。

## Claude Code 驗收（2026-09-24，HEAD `f92c0a6`）— CLOSED（附 1 項缺陷，列入 0005）

- **A（證據驗證器）：** 只讀、不聯網；schema / SHA / arch / summary / privacy / 機密掃描 / worker 欄位 / multi-codex 隔離 / 故障隔離欄位皆有對應檢查與測試。**缺陷 F1：** `provenance` 依賴 `evidence.workflowProvenance`，但 repo 內**沒有任何程式碼產生此欄位**（`grep` 為零），因此連真實 workflow 產出的證據也永遠標成「LOCAL_SCRIPT（非 workflow PASS）」，標示不實用。PASS/FAIL 判定不受影響，故不阻擋；→ 0005 A 修正。
- **B（ARM64 標籤）：** `runs-on: ${{ fromJSON(a && '[..]' || b && '[..]' || '[..]') }}` 的 `&&`/`||` 短路語意與 `inputs` 在 `runs-on` 的可用性皆符合 GitHub Actions 文件；runtime hard gate 保留；audit 正規式同步更新。**殘餘風險：** 此檔僅 `workflow_dispatch`，語法錯誤不會在 push 時使 CI 失敗，實際解析要合併進 `main` 後首次派發才會驗證（`actionlint` 本機不可用，Codex 已如實揭露）。→ 已記入 0005 B 合併後驗證項。
- **C（PR #7 就緒報告）：** 數字皆為指令實測且標明時間截點（當時 775 commits / 151 檔，現因後續 commit 為 781 / 158，屬預期漂移），如實標註當時 CI 為 QUEUED/IN_PROGRESS，未冒稱全綠；「合併為人工閘門」明確。接受。
